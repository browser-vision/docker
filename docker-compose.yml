services:
  vision:
    build: .
    shm_size: 4g
    # network_mode `host` will expose all ports.
    network_mode: "host"
    environment:
      VISION_HTTP_SERVER: "0.0.0.0:3030" # listen on public addr for future port exposing
    restart: unless-stopped
